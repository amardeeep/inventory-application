<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Games</title>
  </head>
  <body>
    <%- include('header') %> <%- include('navbar') %>
    <h2>Games (W.I.P.)</h2>
    <div>
      <ul>
        <% for (let game of games){%>
        <li>
          <div>
            <h3><%= game.gamename %></h3>
            <p>Price : $ <%= game.price %></p>
            <nav>
              <ul>
                <% for (let genre of game.genre) {%>
                <a href=""><li><%=genre.genrename%></li></a>
                <%}%>
              </ul>
            </nav>
          </div>
        </li>
        <%}%>
      </ul>
    </div>
    <%- include('footer') %>
  </body>
</html>
