<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Genres</title>
  </head>
  <body>
    <%- include('header') %> <%- include('navbar') %>
    <h2>Genres (W.I.P.)</h2>
    <div>
      <ul>
        <% for (let genre of genres){%>
        <li>
          <div>
            <h3><%= genre.genrename %></h3>
            <p><a href="/genres/<%=genre.genrename%>">More</a></p>
          </div>
        </li>
        <%}%>
      </ul>
    </div>
    <%- include('footer') %>
  </body>
</html>
